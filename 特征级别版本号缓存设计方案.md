# Aviator 表达式缓存 — 特征级别版本号设计方案

## 一、背景与目标

### 1.1 现状

当前缓存系统使用**全局版本号**控制全量重载：
- 全局版本号变化 → 重新加载所有交易码、所有特征
- 开销大、影响面广、GC 压力高

### 1.2 业务前提

- **1:N 关系**：一个交易码下有多个特征，一个特征只属于一个交易码
- **单交易码特征数量**：最多 1000 条，一般 < 500 条
- **写入方**：特征管理台（配置、测试、发布）
- **读取方**：统一报文加工服务（执行表达式）
- **空交易码清理**：交易码下特征全部删除后，需清理 Redis 中的交易码数据

### 1.3 改造目标

从"全局版本号 + 全量重载"演进为"**特征级别版本号 + 精确增量更新**"：
- 每个交易码有独立版本号（哨兵）
- 每个特征有独立版本号（精确判断）
- 特征变化时，只重载变化的特征，不影响同交易码下的其他特征

---

## 二、Redis 存储结构

### 2.1 Key 设计

```
Key: expr:txn:versions                              Hash    全局交易码版本索引
  Field: {txnCode}
  Value: {txnVersion}

Key: expr:txn:{txnCode}                             Hash    交易码骨架（仅含特征版本号）
  Field: {featureCode}                               String  特征版本号

Key: expr:txn:data:{txnCode}:{featureCode}          String  特征表达式编译字节（Base64 编码）
```

### 2.2 设计说明

| 设计决策 | 原因 |
|----------|------|
| 版本信息（轻量）和数据（重量）分离 | 特征字节 5-10KB，版本号仅几十字节，分开避免大对象传输 |
| 交易码骨架用 Hash | 一次 HGETALL 拿到所有特征版本号，毫秒级完成比对 |
| 特征字节用独立 String Key | 单个特征 5-10KB，需要时按需 GET，不与其他数据耦合 |
| 保留全局版本索引 Hash | 定时轮询时一次拿到所有交易码版本，无需 SCAN |
| **不在交易码 Hash 里存 `_version`** | 全局索引 `expr:txn:versions` 已经有版本号，再存是冗余。同步时先查索引拿版本号，再查 Hash 拿特征版本号，职责清晰 |

---

## 三、写入侧流程（特征管理台）

所有写入操作均通过 Redis 事务（MULTI/EXEC）保证原子性。

### 3.1 新增特征 — putFeature(txnCode, featureCode, expression)

**步骤**：
1. 编译表达式 → 序列化 → Base64 编码
2. Redis 事务执行以下操作：
   - `SET expr:txn:data:{txnCode}:{featureCode} {base64字节}`（特征字节）
   - `HSET expr:txn:{txnCode} {featureCode} 1`（特征版本号初始为 1）
   - `HINCRBY expr:txn:versions {txnCode} 1`（全局索引版本号 +1）

**幂等性**：如果特征已存在，相当于更新，版本号递增。

---

### 3.2 更新特征 — updateFeature(txnCode, featureCode, expression)

**步骤**：
1. 编译表达式 → 序列化 → Base64 编码
2. Redis 事务执行以下操作：
   - `SET expr:txn:data:{txnCode}:{featureCode} {新的base64字节}`（特征字节）
   - `HINCRBY expr:txn:{txnCode} {featureCode} 1`（特征版本号 +1）
   - `HINCRBY expr:txn:versions {txnCode} 1`（全局索引版本号 +1）

**注意**：
- 接口签名增加 `txnCode` 参数（方案A：调用方传入）
- 因为 1:N 关系，特征只属于一个交易码，调用方知道 txnCode

---

### 3.3 删除特征 — removeFeature(txnCode, featureCode)

**步骤**：
1. Redis 事务执行以下操作：
   - `DEL expr:txn:data:{txnCode}:{featureCode}`（删除特征字节）
   - `HDEL expr:txn:{txnCode} {featureCode}`（删除特征版本号）
   - `HINCRBY expr:txn:versions {txnCode} 1`（全局索引版本号 +1）
2. 事务执行后，检查该交易码下是否还有特征
   - `HLEN expr:txn:{txnCode}` 获取剩余字段数
   - 如果为 0：
     - `DEL expr:txn:{txnCode}`（删除整个交易码 Hash）
     - `HDEL expr:txn:versions {txnCode}`（从全局索引移除）

**注意**：
- 去掉 `deleteBytes` 参数，因为特征只属于一个交易码，删了就是真删
- 空交易码清理放在事务后单独判断，因为事务内无法拿到删除后的字段数

---

### 3.4 删除交易码 — removeTxn(txnCode)

**步骤**：
1. 先获取该交易码下的所有特征编码（用于删除字节）
   - `HKEYS expr:txn:{txnCode}` → 拿到所有特征版本号字段
2. 提取特征编码列表
3. Redis 事务执行以下操作：
   - `DEL expr:txn:{txnCode}`（删除交易码 Hash）
   - `HDEL expr:txn:versions {txnCode}`（从全局索引移除）
   - 对每个特征：`DEL expr:txn:data:{txnCode}:{featureCode}`（删除特征字节）

**注意**：删除交易码需要连带删除所有特征字节，可以用 UNLINK 替代 DEL 异步删除，减少事务阻塞时间。

---

### 3.5 事务原子性说明

所有涉及"修改数据 + 递增版本号"的操作都在同一个 MULTI/EXEC 事务中，保证：
- 数据修改和版本号递增同时成功或同时失败
- 不会出现"数据改了但版本号没加"或"版本号加了但数据没改"的情况

---

## 四、读取侧同步流程（统一报文加工服务）

### 4.1 本地缓存结构

```java
/**
 * 交易码表达式上下文（含版本号）
 */
class TxnExpressionContext {
    String txnCode;
    long version;                                    // 交易码版本号
    Map<String, FeatureVersionedExpression> features; // 特征Map，key=featureCode
}

/**
 * 特征表达式（含版本号）
 */
class FeatureVersionedExpression {
    String featureCode;
    long version;            // 特征版本号
    Expression expression;   // 编译后的表达式对象
}
```

**为什么用 Map 不用 List？**
- 精确更新需要快速查找单个特征：`Map.get(featureCode)` 是 O(1)
- 执行时遍历全部特征：`Map.values()` 即可
- 删除特征：`Map.remove(featureCode)` 直接操作

---

### 4.2 启动全量加载 — fullReloadInternal()

**触发时机**：应用启动后（ApplicationReadyEvent）

**流程**：
1. `HGETALL expr:txn:versions` → 拿到所有交易码的版本号
2. 遍历每个交易码：
   - `HGETALL expr:txn:{txnCode}` → 拿到所有特征版本号（不含字节）
   - 解析：所有字段都是 `featureCode → version`
   - 提取所有特征编码
   - **批量取字节**：`MGET expr:txn:data:{txnCode}:feat1, expr:txn:data:{txnCode}:feat2, ...`
   - 逐个反序列化字节 → Expression 对象
   - 组装 `TxnExpressionContext`（含 Map）
3. 构建完整的 `localCache` 和 `localVersions`
4. 原子替换：`this.localCache = newCache`

**说明**：启动时是冷加载，需要拉全部数据，所以用 MGET 批量取字节。运行时同步是热路径，按需取单个字节。

---

### 4.3 定时增量轮询 — pollAndReload()

**触发时机**：每 10 秒（可配置）

**流程**：
1. `HGETALL expr:txn:versions` → 拿到 Redis 上所有交易码的版本号
2. 与本地 `localVersions` 比对，找出变化清单：
   - 新增的：Redis 有、本地没有 → 需要加载
   - 删除的：本地有、Redis 没有 → 需要移除
   - 修改的：版本号不一致 → 需要精确同步
3. 如无变化，直接返回
4. 尝试获取 `reloadLock`（tryLock，拿不到就放弃）
5. 加锁后再次比对（双重检查）
6. 对每个变化的交易码执行"单交易码精确同步"
7. 释放锁

---

### 4.4 单交易码精确同步 — syncTxnFeatures(txnCode)

**核心逻辑**：先对比特征版本号（轻量级），只对变化了的特征 GET 字节（重量级）。

**流程**：
```
输入：txnCode
输出：更新后的 TxnExpressionContext

1. HGETALL expr:txn:{txnCode}
   → 拿到：所有 {featureCode} → {version} 字段
   → 数据量：约 特征数 × 几十字节 ≈ 20KB（500特征）

2. 解析 Redis 数据（所有字段都是特征版本号）：
   └─ redisFeatureVersions = {featureCode → version}

3. 与本地缓存的该交易码比对：

   3.1 新增的特征（Redis有、本地没有）
   3.2 修改的特征（版本号不一致）
   └─ 合并到 changedFeatures 列表

   3.3 删除的特征（本地有、Redis没有）
   └─ 记录到 deletedFeatures 列表

4. 对 changedFeatures 中的每个特征：
   ├─ GET expr:txn:data:{txnCode}:{featureCode}     按需取单个特征字节
   ├─ 反序列化 → 新的 Expression
   └─ 放入本地 Map，更新特征版本号

5. 对 deletedFeatures 中的每个特征：
   └─ 从本地 Map 中移除

6. 更新交易码版本号

7. 返回更新后的上下文
```

**伪代码**：
```java
TxnExpressionContext syncTxnFeatures(String txnCode) {
    Map<String, String> redisFeatVersions = hashOps.entries("expr:txn:" + txnCode);
    if (redisFeatVersions.isEmpty()) {
        // 交易码不存在，删除本地缓存
        localCache.remove(txnCode);
        localVersions.remove(txnCode);
        return null;
    }

    // 所有字段都是特征版本号
    Map<String, Long> redisFeatVersionMap = new HashMap<>();
    for (Map.Entry<String, String> e : redisFeatVersions.entrySet()) {
        redisFeatVersionMap.put(e.getKey(), Long.parseLong(e.getValue()));
    }

    // 获取或创建本地上下文
    TxnExpressionContext ctx = localCache.get(txnCode);
    Map<String, FeatureVersionedExpression> localFeatures;
    if (ctx == null) {
        ctx = new TxnExpressionContext(txnCode, 0L, new HashMap<>());
        localFeatures = ctx.getFeatures();
    } else {
        localFeatures = ctx.getFeatures();
    }

    // 找出新增和修改的特征
    List<String> changedFeatures = new ArrayList<>();
    for (Map.Entry<String, Long> e : redisFeatVersionMap.entrySet()) {
        String featCode = e.getKey();
        long newFeatVersion = e.getValue();
        FeatureVersionedExpression local = localFeatures.get(featCode);
        if (local == null || local.getVersion() != newFeatVersion) {
            changedFeatures.add(featCode);
        }
    }

    // 按需取变化的特征字节（重量级操作）
    if (!changedFeatures.isEmpty()) {
        List<String> byteKeys = changedFeatures.stream()
            .map(fc -> "expr:txn:data:" + txnCode + ":" + fc)
            .collect(Collectors.toList());
        List<String> byteValues = redisTemplate.opsForValue().multiGet(byteKeys);

        for (int i = 0; i < changedFeatures.size(); i++) {
            String featCode = changedFeatures.get(i);
            String base64 = byteValues.get(i);
            if (base64 == null) continue;
            byte[] bytes = Base64.getDecoder().decode(base64);
            Expression expr = deserialize(bytes, evaluator);
            localFeatures.put(featCode, new FeatureVersionedExpression(
                featCode, redisFeatVersionMap.get(featCode), expr));
        }
    }

    // 找出删除的特征
    Iterator<Map.Entry<String, FeatureVersionedExpression>> it = localFeatures.entrySet().iterator();
    while (it.hasNext()) {
        if (!redisFeatVersionMap.containsKey(it.next().getKey())) {
            it.remove();
        }
    }

    return ctx;
}
```

**数据量对比**：
| 操作 | 旧设计（一个Hash含字节） | 新设计（骨架+字节分离） |
|------|:----------------------:|:---------------------:|
| 版本比对 HGETALL | 5MB（500特征×10KB） | 20KB（仅版本号） |
| 加载变化特征 | 已包含在 HGETALL 中 | 单独 MGET（1个特征10KB） |
| 一次变化只动1个特征 | 反序列化 500次 | 反序列化 1次，传输 1次 |

---

### 4.5 查询时按需刷新 — getByTxnCode(txnCode)

**流程**：
1. 如果 `reloadLock` 被锁，直接返回本地缓存（有人在更新，先凑合用旧数据）
2. `HGET expr:txn:versions {txnCode}` → 拿到 Redis 版本号
3. 与本地 `localVersions` 中该交易码的版本号比对
   - 版本一致 → 返回本地缓存
   - 版本不一致 → 触发单交易码精确同步（同 4.4）
4. 返回结果

---

### 4.6 并发控制

| 机制 | 说明 |
|------|------|
| `volatile localCache` | 读线程无锁，写线程整体替换 |
| `ConcurrentHashMap<String, Long> localVersions` | 版本号用并发 Map，支持并发读 |
| `ReentrantLock reloadLock` | 全局一把锁，任何时候只有一个重载/同步在执行 |
| tryLock 非阻塞 | 拿不到锁就放弃，不阻塞查询线程 |
| 双重检查 | 加锁后再次比对版本，避免重复加载 |

---

## 五、涉及修改的文件

### 5.1 ExprRedisKeys.java

| 改动项 | 说明 |
|--------|------|
| 删除 `VERSION` 常量 | 不再使用全局版本号 |
| 删除 `TXN_PREFIX` | 交易码索引不再是 Set，改成 Hash |
| 删除 `FEATURE_PREFIX` | 特征字节不再是独立 Key，独立存储 |
| 删除 `featureKey()` 方法 | 不再需要单独构建特征 Key |
| 删除 `txnKey()` 方法 | 改成新的命名 |
| 新增 `TXN_VERSIONS_KEY` | 全局交易码版本索引 Hash 的 Key |
| 新增 `TXN_SKELETON_PREFIX` | 交易码骨架 Hash 的 Key 前缀 |
| 新增 `TXN_DATA_PREFIX` | 交易码数据（特征字节）的 Key 前缀 |
| 新增 `txnSkeletonKey(txnCode)` | 构建交易码骨架 Hash 的 Key |
| 新增 `featureDataKey(txnCode, featureCode)` | 构建特征字节 Key |

---

### 5.2 TxnExpressionContext.java

| 改动项 | 说明 |
|--------|------|
| `features` 类型从 `List<FeatureExpression>` 改为 `Map<String, FeatureVersionedExpression>` | 便于精确查找和更新 |
| 内部类 `FeatureExpression` 重命名为 `FeatureVersionedExpression` | 名称更准确 |
| 新增 `version` 字段（long） | 特征级别版本号 |
| 更新 equals/hashCode/toString | 加入 version 字段 |

---

### 5.3 AviatorRedisExpressionCache.java

**核心改动：**

| 方法 | 改动说明 |
|------|----------|
| `localCache` 类型 | `Map<String, List<FeatureExpression>>` → `Map<String, TxnExpressionContext>` |
| `localVersion` | `AtomicLong` → `ConcurrentHashMap<String, Long>` |
| `pollAndReload()` | 从"全局版本比对+全量重载"改为"HGETALL比对+逐个交易码精确同步" |
| `fullReloadInternal()` | 从 SCAN 方式改为 HGETALL 方式 |
| `ensureFreshCache()` | 从"全局版本检查"改为"单交易码版本检查" |
| `loadFromRedis()` | 改为 `syncTxnFeatures()`，精确增量同步 |
| `putFeature()` | 改为 Hash 操作，版本号递增 |
| `updateFeature()` | 增加 `txnCode` 参数，Hash 操作 |
| `removeFeature()` | 去掉 `deleteBytes` 参数，空交易码自动清理 |
| `removeTxn()` | 改为 Hash 删除 + 索引删除 |
| 新增 `syncTxnFeatures(txnCode)` | 单交易码精确同步核心方法 |
| 新增 `getAllTxnVersions()` | 获取所有交易码版本号（HGETALL） |
| 新增 `parseTxnHashData()` | 解析交易码 Hash 数据，分离版本和特征 |

---

### 5.4 RedisUtils.java

| 改动项 | 说明 |
|--------|------|
| 新增 Hash 相关操作方法 | `hGet`, `hSet`, `hGetAll`, `hDel`, `hIncrBy` 等 |
| 调整 `executeInTransaction` | 适配 Hash 操作（通过 RedisConnection 调用 hSet/hDel/hIncrBy 等） |

---

## 六、性能对比

| 场景 | 全局版本号（现状） | 交易码级别版本 | 特征级别版本（本方案） |
|------|:-----------------:|:--------------:|:---------------------:|
| 一个特征变化 | 全量重载所有交易码所有特征 | 重载该交易码所有特征 | 只重载变化的那一个特征 |
| 新增一个特征 | 全量重载 | 重载该交易码所有特征 | 只反序列化新增的那一个 |
| 删除一个特征 | 全量重载 | 重载该交易码所有特征 | 直接删除，无反序列化 |
| 一个新交易码上线 | 全量重载 | 只加载新交易码 | 只加载新交易码 |
| 500特征/交易码，变1个 | 反序列化 500×N 次 | 反序列化 500 次 | 反序列化 1 次 |
| GC 压力 | 高（大量对象重建） | 中（该交易码全部重建） | 低（只重建变化的） |
| 代码复杂度 | 低 | 中 | 高 |

---

## 七、风险与注意事项

### 7.1 版本号一致性

- 交易码版本号只在 `expr:txn:versions` 一处存储，不存在多副本一致性问题
- 特征版本号存在 `expr:txn:{txnCode}` Hash 中
- 所有写入通过 Redis 事务保证：字节变更 + 版本号递增 原子完成
- 单 Key 损坏时，直接从 Redis 重读即可恢复，不需要多源对账

### 7.2 空交易码清理时机

- 删除特征后，需要判断交易码下是否还有其他特征
- 如果没有了，需要删除交易码 Hash 和版本索引
- 这个判断在事务外执行（事务内拿不到删除后的状态）
- 极端情况下（并发删除最后两个特征）可能漏删，但不影响正确性，下次轮询时会发现并清理

### 7.3 HGETALL 数据量

- 单交易码 500 个特征，每个 1KB，约 500KB/次 HGETALL
- Redis 局域网内 500KB 传输只需几毫秒，没问题
- 如果同时变化的交易码很多，注意网络带宽和 Redis 压力
- 建议：轮询时如果变化的交易码 > 10 个，分批处理

### 7.4 特征编码命名限制

- 特征编码中不能包含冒号 `:`，因为 `expr:txn:data:{txnCode}:{featureCode}` 这个 Key 结构用冒号分隔
- 如果特征编码可能有冒号，需要换分隔符（如 `|`）或对特征编码做 URL-encode

---

## 八、适用场景判断

### ✅ 推荐使用本方案的场景

- 单交易码特征数量较多（>50）
- 每次发布只改少数特征
- 发布频率较高（每天多次）
- 对延迟敏感，不能接受全量重载
- 有精力维护相对复杂的代码

### ⚠️ 不推荐的场景

- 每次发布都是全量发布 → 精确更新没意义
- 交易码少、特征少 → 全量重载开销本来就小
- 追求代码最简 → 交易码级别足够

---

## 九、实施步骤建议

1. **第一步**：修改 `ExprRedisKeys` 常量定义
2. **第二步**：修改 `TxnExpressionContext` 数据结构（Map + 特征版本号）
3. **第三步**：修改 `RedisUtils`，增加 Hash 操作方法
4. **第四步**：重写 `AviatorRedisExpressionCache` 核心逻辑
   - 先写写入方法（put/update/remove）
   - 再写读取方法（同步、查询）
   - 最后写定时轮询和启动加载
5. **第五步**：编写单元测试
   - 新增特征 → 版本号验证
   - 更新特征 → 精确更新验证（其他特征不受影响）
   - 删除特征 → 清理验证
   - 空交易码 → 自动清理验证
   - 并发场景 → 一致性验证
6. **第六步**：集成测试
   - 特征管理台发布 → 报文加工服务同步验证
